# Development environment
# Full URL because we're not using nginx proxy in dev

# For LOCAL development (python run.py on port 8000):
VITE_API_BASE_URL=http://localhost:8000/api

# For DOCKER backend (docker-compose up on port 8001):
# VITE_API_BASE_URL=http://localhost:8001/api

VITE_APP_NAME=ERP Inventory Manager (Dev)
